<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team404.reply.mapper.ReplyMapper">

	<insert id="regist">
		insert into FREEREPLY(rno , bno , reply , replyID , replyPW , replyDate)
		values(FREEREPLY_SEQ.nextval, #{bno}, #{reply}, #{replyID} , #{replyPW} , sysdate)
	</insert>
	
	<select id="getList" resultType="FreeReplyVO">
		select bno,rno,reply,replyID,replyDate,updateDate from FREEREPLY where bno =#{bno} order by rno desc
	</select>







</mapper>